    export const postOrder = (request, response) => {
    response.send("food/order Post huselt irlee");
    }

    export const getOrder = (request, response) => {
        response.send("food/order Get huselt irlee");
    }

    export const getSignUp = (request, response) => {
        response.send("auth/signup Get huselt irlee");
    }
    export const getResetPasswordRequest = (request, response) => {
    response.send("auth/reset-password-request Get huselt irlee");
    }

    export const getVerifyPassword = (request, response) => {
        response.send("auth/verify-password Get huselt irlee");
    }

    export const getResetPassword = (request, response) => {
        response.send("auth/reset-password Get huselt irlee");
    }
    
